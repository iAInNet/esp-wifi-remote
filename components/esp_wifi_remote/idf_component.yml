version: 1.3.3
url: https://github.com/iAInNet/esp-wifi-remote
description: Utility wrapper for esp_wifi functionality on remote targets
dependencies:
  idf:
    version: '>=5.3'
  espressif/esp_hosted:
    version: b3307ef084deb253489da7eac50d891c5cb2c96b
    rules:
    - if: "target in [esp32h2, esp32p4]"
  espressif/wifi_remote_over_eppp:
    version: '>=0.1'
    # TODO: Uncomment this once conditional dependencies are supported
    # matches:
    #   - if: "idf_version <5.5"
    #   - if: "$CONFIG{ESP_WIFI_REMOTE_LIBRARY_EPPP} == True"
